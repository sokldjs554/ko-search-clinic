"""한국어 검색 운영 지식베이스 — 프롬프트에 박혀 있던 것을 꺼내 온 곳.

## 왜 이것이 따로 있는가

지금까지 이 지식은 `prompts.SYSTEM_PROMPT` 안에 통째로 박혀 있었다. 그 방식은
두 가지 값을 치른다:

    1. **매 호출마다 토큰 비용을 낸다.** 지식은 안 변하는데 매번 실어 보낸다.
    2. **확장되지 않는다.** 계열이 다섯이라 프롬프트에 들어갔지, 실무의
       사례집이 수백 건이면 넣을 자리가 없다.

RAG는 이 둘을 바꾼다 — 필요한 문서만 골라 그때만 싣는다. 그 교체가 실제로
성립하는지가 이 저장소에서 재려는 것이다(docs/EVALUATION.md).

## 무엇을 넣고 무엇을 넣지 않았는가

**일반 원칙만 넣는다.** "`아답터`는 사전 등록 후 동의어"처럼 평가셋 질의의
답을 그대로 적으면, 그것은 지식베이스가 아니라 **정답표**다. 그런 문서로
잰 성적은 검색이 잘 됐다는 뜻이 아니라 답을 흘렸다는 뜻이다.

그래서 여기 문서들은 "분석기가 단어를 음절로 쪼개면 사전 등록이 선행되어야
한다"처럼 **계열 수준의 원칙**만 담는다. 어떤 질의에 어떤 처방인지는 여전히
의사가 도구로 증거를 모아 판단해야 한다.

문서 본문은 nori·Kiwi 문서와 이 프로젝트의 실측에서 확인한 내용을 정리한
것이고, 출처는 각 문서의 `source`에 남긴다.
"""

from __future__ import annotations

from dataclasses import dataclass, field


@dataclass(frozen=True)
class KnowledgeDoc:
    doc_id: str
    title: str
    body: str
    source: str
    # 이 문서가 근거가 되는 실패 계열. 검색 품질을 오프라인으로 채점할 때
    # 정답 라벨로 쓴다 — 어느 계열의 질의에 이 문서가 나와야 하는가.
    families: tuple[str, ...] = field(default=())

    @property
    def text(self) -> str:
        return f"{self.title}\n{self.body}"


def _d(doc_id, title, body, source, families=()):
    return KnowledgeDoc(doc_id, title, " ".join(body.split()), source, tuple(families))


KNOWLEDGE: list[KnowledgeDoc] = [
    # ---------------------------------------------------- 분석기 일반
    _d(
        "K001",
        "한국어 형태소 분석기의 짧은 음절 오분석",
        """
        한국어 형태소 분석기는 사전에 없는 외래어를 만나면 아는 형태소들의
        조합으로 쪼개려 한다. 이때 한 음절짜리 조각이 엉뚱한 품사로 읽히는
        일이 흔하다. 첫 음절이 감탄사(IC)로 읽히거나, 숫자를 뜻하는 수사(NR)로
        읽히면 내용어 필터에서 통째로 버려진다. 결과적으로 질의에 남는 토큰이
        원래 단어와 아무 관계가 없어지고, 색인의 어떤 문서와도 매칭되지 않는다.
        분석 결과에서 **버려진 토큰의 품사**를 보면 이 현상을 바로 확인할 수 있다.
        """,
        "Kiwi 품사 태그셋 / 프로젝트 실측",
        ("garbage_split",),
    ),
    _d(
        "K002",
        "사용자 사전 등록이 동의어보다 먼저인 이유",
        """
        분석기가 단어를 음절 단위로 부수는 경우, 동의어만 등록하는 것은 무효다.
        동의어 필터는 **토큰이 생긴 뒤에** 그 토큰을 치환한다. 질의를 분석한
        결과에 그 단어의 토큰이 애초에 존재하지 않으면 치환할 대상이 없다.
        그래서 순서가 정해져 있다: 먼저 사용자 사전에 그 단어를 원자 토큰으로
        등록해 분해를 막고, 그 다음에 표준형과 동의어로 잇는다. 이것은 유사도
        판단의 문제가 아니라 **절차의 문제**이므로, 후보 단어를 아무리 잘 찾아도
        순서를 틀리면 고쳐지지 않는다.
        """,
        "Elasticsearch nori 분석 체인 / 프로젝트 실측",
        ("garbage_split",),
    ),
    _d(
        "K003",
        "nori 사용자 사전(user_dictionary_rules)",
        """
        nori_tokenizer는 user_dictionary_rules로 사용자 사전 항목을 받는다.
        한 줄에 한 단어이며, 등록된 단어는 그 자체로 하나의 토큰이 되어
        분해되지 않는다. 공백으로 구분해 분해 형태를 함께 적으면 복합어의
        분해 방식을 지정할 수도 있다. 사전을 바꾸면 색인을 다시 만들어야
        반영된다 — 색인 시점의 분석 결과가 이미 저장돼 있기 때문이다.
        """,
        "Elasticsearch nori 플러그인 문서",
        ("garbage_split", "compound_locked"),
    ),
    _d(
        "K004",
        "nori decompound_mode와 복합어 색인",
        """
        nori_tokenizer의 decompound_mode는 복합명사를 어떻게 색인할지 정한다.
        none은 복합어를 통째로 두고, discard는 구성 요소만 남기며, mixed는
        복합어와 구성 요소를 모두 색인한다. none이나 사전에 통짜로 등록된
        복합어는 그 부분어로 검색했을 때 매칭되지 않는다. 부분어 검색을
        지원하려면 mixed를 쓰거나 복합어의 분해 형태를 명시해야 한다. 다만
        mixed는 색인 크기가 커지고, 짧은 구성 요소가 무관한 질의를 끌어올 수
        있다.
        """,
        "Elasticsearch nori 플러그인 문서",
        ("compound_locked",),
    ),
    _d(
        "K005",
        "synonym_graph 항목은 단일 토큰이어야 한다",
        """
        synonym_graph 필터는 동의어 항목을 분석기에 통과시킨 뒤 사용하는데,
        이때 항목이 두 개 이상의 토큰으로 쪼개지면 인덱스 생성 자체가
        거부된다. 오류 메시지는 해당 항목이 분석된 토큰과 함께 위치 증가값이
        1이 아니라는 형태로 나온다. 따라서 동의어로 쓸 단어가 분석기에서
        쪼개진다면, 먼저 사용자 사전에 원자 토큰으로 등록해 두어야 동의어
        설정이 성립한다.
        """,
        "Elasticsearch synonym_graph 문서 / 프로젝트 실측",
        ("garbage_split", "spelling_variant"),
    ),
    # ---------------------------------------------------- 실패 계열
    _d(
        "K006",
        "외래어 표기 변형",
        """
        외래어는 표준 표기와 관용 표기가 함께 쓰인다. 두 표기는 자모 수준에서
        한두 글자만 다른 경우가 많아, 한글을 초성·중성·종성으로 분해한 뒤
        편집 거리를 재면 가려낼 수 있다. 색인에는 보통 표준 표기만 들어가
        있으므로, 사용자가 관용 표기로 검색하면 0건이 된다. 처방은 두 표기를
        동의어로 잇는 것이고, 동의어 그룹의 대표는 **색인에 실제로 존재하는
        표기**여야 한다. 색인에 없는 말을 대표로 삼으면 어떤 문서와도 이어지지
        않는다.
        """,
        "국립국어원 외래어 표기법 / 프로젝트 실측",
        ("spelling_variant",),
    ),
    _d(
        "K007",
        "영한 혼용 표기",
        """
        같은 대상을 로마자로도 한글 음차로도 적는 용어가 있다. 문자 체계가
        다르므로 자모 편집 거리는 0에 가깝게 나오고, 형태 기반 방법으로는
        절대 이어지지 않는다. 문서와 질의가 서로 다른 문자 체계를 쓰는지
        원문 확인으로 대조하는 것이 진단의 핵심이며, 처방은 두 표기를 동의어로
        잇는 것이다. 다국어 임베딩도 이 경우에 특히 약한데, 로마자 토큰이
        한국어 토큰 군집 밖에 놓이기 때문이다.
        """,
        "프로젝트 실측 (임베딩 분포 분석)",
        ("cross_script",),
    ),
    _d(
        "K008",
        "유의어 단절 — 형태가 다른 같은 뜻",
        """
        표기 변형과 달리 유의어는 형태에 공통점이 없다. 구어체 표현과 상품
        카테고리명, 옛말과 요즘말이 이런 관계가 된다. 자모 거리로도 못 잡고,
        문자 체계도 같아서 영한 혼용 단서도 없다. 색인의 어휘 목록을 훑어
        같은 뜻을 가리키는 다른 말이 있는지 확인해야 하며, 처방은 동의어다.
        다만 유의어는 표기 변형보다 의미 폭이 넓어 과잉 일반화 위험이 크므로
        범위를 좁게 잡아야 한다.
        """,
        "프로젝트 실측",
        ("synonym_gap",),
    ),
    _d(
        "K009",
        "복합어가 통짜로 색인될 때",
        """
        복합명사가 하나의 토큰으로 색인되면 그 부분어로는 검색되지 않는다.
        사용자는 부분어로 검색하는 일이 많으므로 0건이 흔히 발생한다. 진단은
        간단하다 — 검색은 0건인데 원문 grep에는 그 부분어를 포함한 복합어가
        있는 경우다. 처방은 복합어를 구성 요소로 분해해 함께 색인하도록
        확장하는 것이며, 분해한 구성 요소가 너무 짧고 흔한 말이면 무관한
        질의를 끌어오므로 주의해야 한다.
        """,
        "프로젝트 실측",
        ("compound_locked",),
    ),
    # ---------------------------------------------------- 처방 원칙
    _d(
        "K010",
        "동의어 과잉 일반화의 위험",
        """
        동의어 그룹에 짧고 다의적인 말을 포함시키면 무관한 문서가 대량으로
        끌려 들어온다. 한 음절 또는 두 음절의 흔한 말은 여러 복합어의
        구성 요소이기 때문이다. 하나의 질의를 고치려다 이미 잘 되던 여러
        질의의 정밀도를 떨어뜨리는 것이 이 실수의 전형적인 결과다. 처방은
        표적 질의를 고치는 데 필요한 최소 범위여야 하고, 넓힐수록 회귀
        위험이 지수적으로 커진다.
        """,
        "프로젝트 실측 (회귀 게이트 기각 사례)",
        ("spelling_variant", "synonym_gap", "cross_script"),
    ),
    _d(
        "K011",
        "검색 0건과 쓰레기 유입은 다른 실패다",
        """
        검색 실패에는 두 가지 모양이 있다. 하나는 결과가 0건인 경우이고,
        다른 하나는 결과가 나오긴 하는데 전부 무관한 문서인 경우다. 후자가
        더 위험하다 — 지표상 "검색은 된다"로 보이고, 사용자 민원이 없으면
        아무도 모른다. 단어가 잘못 분해돼 생긴 조각이 다른 문서의 토큰과
        우연히 겹칠 때 이런 일이 생긴다. 그래서 결과 수만 보지 말고 상위
        결과가 실제로 관련 있는지 함께 봐야 한다.
        """,
        "프로젝트 실측",
        ("garbage_split",),
    ),
    _d(
        "K012",
        "진단의 결정적 증거 — 검색과 원문 대조",
        """
        검색 결과가 0건일 때 원인은 둘 중 하나다. 문서가 정말 없거나,
        문서는 있는데 분석이 실패한 것이다. 이 둘을 가르는 방법은 분석기를
        우회해 원문에서 직접 문자열을 찾아보는 것이다. 원문에는 있는데
        검색이 0건이라면 색인 대상 자체는 존재하며 문제는 분석 단계에 있다.
        이 대조 없이 진단하면 "그런 상품이 없나 보다"에서 멈추게 된다.
        확인 후에는 질의와 문서가 각각 어떤 토큰으로 분석되는지 대조해
        불일치 지점을 찾는다.
        """,
        "프로젝트 실측",
        ("garbage_split", "compound_locked", "spelling_variant", "synonym_gap", "cross_script"),
    ),
    # ---------------------------------------------------- 평가·검증
    _d(
        "K013",
        "검색 품질 지표 — nDCG만으로는 부족하다",
        """
        nDCG는 정답 문서가 상위에 있는지를 본다. 그래서 정답 아래에 무관한
        문서가 쌓여도 크게 벌하지 않는다. 정밀도(precision@k)를 함께 봐야
        쓰레기 유입을 잡을 수 있다. 재현율은 정답을 얼마나 놓쳤는지를 보는데,
        결과 수가 줄면서 재현율은 유지되고 정밀도가 오르는 변화가 좋은
        패치의 전형적인 모습이다. 즉 검색 품질은 많이 찾는 것이 아니라
        맞게 찾는 것이다.
        """,
        "정보검색 표준 지표",
        (),
    ),
    _d(
        "K014",
        "사전 수정은 회귀를 만든다 — 전수 재평가가 필요한 이유",
        """
        사용자 사전이나 동의어를 고치면 그 효과는 표적 질의에만 머무르지
        않는다. 토큰이 바뀌면 문서 빈도가 바뀌고, 문서 빈도가 바뀌면 관련
        없어 보이는 질의의 점수 순위까지 이동한다. 그래서 한 질의를 고친 뒤
        전체 평가셋을 다시 돌려보지 않으면 어디가 망가졌는지 알 수 없다.
        현업에서 검색이 조용히 나빠지는 경로가 정확히 이것이다 — 누군가
        민원을 받고 사전을 고치고, 그 수정이 다른 검색을 깨뜨렸는지는
        아무도 확인하지 않는다.
        """,
        "프로젝트 실측 (BM25 문서빈도 변화)",
        (),
    ),
    _d(
        "K015",
        "BM25에서 문서 빈도가 점수에 미치는 영향",
        """
        BM25 점수는 용어의 역문서빈도(IDF)에 비례한다. 어떤 용어를 포함한
        문서가 많아질수록 그 용어의 변별력이 떨어져 점수 기여가 줄어든다.
        동의어를 추가하면 해당 용어를 가진 문서 수가 늘어나므로, 그 용어를
        쓰는 다른 질의들의 점수가 함께 내려간다. 사전 한 줄을 고쳤을 때
        전혀 무관해 보이는 질의가 흔들리는 이유가 여기 있다.
        """,
        "Lucene BM25 구현 / 프로젝트 실측",
        (),
    ),
    _d(
        "K016",
        "nori 품사 필터(nori_part_of_speech)와 내용어",
        """
        nori_part_of_speech 필터는 지정한 품사 태그의 토큰을 색인에서
        제거한다. 조사, 어미, 감탄사처럼 검색에 도움이 되지 않는 품사가
        기본 제거 대상이다. 이 필터는 유용하지만 부작용이 있다 — 분석기가
        어떤 음절을 감탄사로 **오인**한 경우, 그 오인된 토큰이 조용히
        사라진다. 분석 결과만 보면 토큰이 왜 없는지 알 수 없으므로,
        어떤 토큰이 어떤 품사로 제거됐는지 함께 확인할 수 있어야 진단이
        가능하다.
        """,
        "Elasticsearch nori 플러그인 문서 / 프로젝트 실측",
        ("garbage_split",),
    ),
]


def load_knowledge() -> list[KnowledgeDoc]:
    return list(KNOWLEDGE)


def families_covered() -> set[str]:
    return {f for d in KNOWLEDGE for f in d.families}
